# Visual References

These screenshots and portraits are **design references**, not copy targets.

## Structure references

Expected filenames:

- `skills-tech-stack-reference.png` — Tech Stack grid layout inspiration
- `projects-feaon-reference.png` — selected-project list/detail interaction inspiration
- `contact-reference.png` — two-column Contact composition
- `footer-reference.png` — multi-column Footer composition
- `resume-library-reference.png` — left navigation + content catalog inspiration
- `resume-card-reference.png` — structured resume entry/card inspiration
- `hero-about-reference.png` — hero/about split layout inspiration

## Portraits

Expected filenames:

- `portrait-slider-01.jpg` — About right-side slider
- `portrait-slider-02.jpg` — About right-side slider
- `portrait-hero-banner.jpg` — Home hero/banner

## Repository asset status

The planning/spec files are committed. The binary screenshots and portrait files listed above are still pending import into this directory because the GitHub connector used to initialize the repository does not provide a practical local-binary upload path for these conversation attachments.

The files already exist in the planning archive prepared during requirement gathering. Import them before implementing UI issues that depend on visual references, especially #7–#10 and #13. The filenames above are canonical so implementation docs/issues do not need to change after the binary assets are added.

## Rule

Do not reproduce colors, typography, decoration, or branding verbatim from references. Extract interaction and information hierarchy, then apply the portfolio's unified design system.
